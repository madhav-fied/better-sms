# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/sms

# Auth
SESSION_TTL_DAYS=37
SUPERADMIN_API_KEY=dev-superadmin-key
FRONTEND_URL=http://localhost:3000
PASSWORD_RESET_TTL_HOURS=24

# Email (password reset). In dev, links are logged to the server console.
EMAIL_DEV_MODE=true
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_FROM=
SMTP_USE_TLS=true
